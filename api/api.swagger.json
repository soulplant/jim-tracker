{
  "swagger": "2.0",
  "info": {
    "title": "api.proto",
    "version": "version not set"
  },
  "schemes": [
    "http",
    "https"
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/v1/fetch-all": {
      "get": {
        "operationId": "FetchAll",
        "responses": {
          "200": {
            "description": "",
            "schema": {
              "$ref": "#/definitions/apiFetchAllResponse"
            }
          }
        },
        "tags": [
          "ApiService"
        ]
      }
    },
    "/v1/record": {
      "get": {
        "operationId": "RecordDelivery",
        "responses": {
          "200": {
            "description": "",
            "schema": {
              "$ref": "#/definitions/apiRecordDeliveryResponse"
            }
          }
        },
        "parameters": [
          {
            "name": "delivery.date",
            "description": "The date this delivery is for in YYYYMMDD.",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "delivery.time.hour",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "delivery.time.minute",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "delivery.time.second",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          }
        ],
        "tags": [
          "ApiService"
        ]
      }
    }
  },
  "definitions": {
    "apiDelivery": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "The date this delivery is for in YYYYMMDD."
        },
        "time": {
          "$ref": "#/definitions/apiLocalTime",
          "description": "When the delivery took place."
        }
      }
    },
    "apiFetchAllResponse": {
      "type": "object",
      "properties": {
        "delivery": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/apiDelivery"
          }
        }
      }
    },
    "apiLocalTime": {
      "type": "object",
      "properties": {
        "hour": {
          "type": "integer",
          "format": "int32"
        },
        "minute": {
          "type": "integer",
          "format": "int32"
        },
        "second": {
          "type": "integer",
          "format": "int32"
        }
      }
    },
    "apiRecordDeliveryResponse": {
      "type": "object"
    }
  }
}
